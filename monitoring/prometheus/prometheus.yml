global:
  scrape_interval: 5s # Ambil data setiap 5 detik

scrape_configs:
  - job_name: "fastapi-backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["backend:8000"] # Hostname sesuai nama service di docker-compose
